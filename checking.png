from tkinter import *
class NavigationPanel:

    def __init__(self, u_id):
        self.panel = Tk()

        

        self.canvas = Canvas(self.panel, height=800, width=1500, bg="red")
        self.canvas.place(x=0, y=0)


        self.canvas1 = Canvas(self.canvas, height=400, width=1300)
        self.canvas1.place(x=20, y=20)

       

        self.panel.mainloop()

if __name__ == "__main__":
    d = NavigationPanel(0)
